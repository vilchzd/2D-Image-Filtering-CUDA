#include <iostream>
#include <fstream>

using namespace std;

int main () {
    image.open()
    new.image.open()

    string type = "", width = "", height = "", RGB = "";
    image >> type;
    image >> width;
    image >> height;
    image >> RGB;

    newimage << type << endl;
    newimage << width << "" << height endl;
    newimage << RGB << endl;

    return 0; 
}